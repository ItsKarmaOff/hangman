#!/bin/bash

# Definition of the path to the hangman executable file
HANGMAN="./hangman"

# Path to the test file
TEST_FILE="tests/test_hangman.txt"

# Default number of attempts
TRIES="10"

# Tests with different options
echo "Test without specifying the number of attempts:"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="1"
echo "Test specifying the number of attempts: $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="2"
echo "Test specifying the number of attempts: $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="3"
echo "Test specifying the number of attempts: $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="4"
echo "Test specifying the number of attempts: $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="5"
echo "Test specifying the number of attempts: $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="6"
echo "Test specifying the number of attempts: $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="7"
echo "Test specifying the number of attempts: $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="8"
echo "Test specifying the number of attempts: $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="9"
echo "Test specifying the number of attempts: $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="10"
echo "Test specifying the number of attempts: $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="11"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="12"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="13"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="14"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="15"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="16"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="17"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="18"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="19"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="20"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="21"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="22"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="23"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="24"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="25"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"

TRIES="26"
echo "Test specifying the number of attempts : $TRIES"
cat "$TEST_FILE" | "$HANGMAN" "$1" "$TRIES"
echo "------------------"
